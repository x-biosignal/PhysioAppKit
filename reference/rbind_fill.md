# Row-bind two data.frames with differing columns (fill missing with NA)

Row-bind two data.frames with differing columns (fill missing with NA)

## Usage

``` r
rbind_fill(a, b)
```

## Arguments

- a, b:

  data.frames. @return their union-column rbind. @export
